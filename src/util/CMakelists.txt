set(HEADER_FILES 
		LogUtilities.h
		GeneralUtilities.h
		DebuggerCommand.h
		CommandLineArgument.h
	)

set(SOURCE_FILES 
		LogUtilities.cpp
		DebuggerCommand.cpp
		CommandLineArgument.cpp
		)

add_library(gbcemu_util 
		${SOURCE_FILES}
		${HEADER_FILES}
	)