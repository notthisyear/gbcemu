set(HEADER_FILES 
		LogUtilities.h
		GeneralUtilities.h
		DebuggerCommand.h
		CommandLineArgument.h
	)

set(SOURCE_FILES 
		LogUtilities.cpp
		CommandLineArgument.cpp
	)

add_library(gbcemu_util 
		${SOURCE_FILES}
		${HEADER_FILES}
	)

#set_target_properties(gbcemu_util PROPERTIES LINKER_LANGUAGE CXX)

# Libraries
target_link_libraries(gbcemu_util PRIVATE spdlog::spdlog)